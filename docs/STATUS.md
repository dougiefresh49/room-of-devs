# Room of Devs — STATUS

Thin index (fleet `html-status` skill): one line per entry, content lives
in the linked docs. Publish with `pnpm docs:publish`. History before
2026-08-29 (the old "Now" section) is in git: `git log -p docs/STATUS.md`.

_Last updated: 2026-08-31 (decision interview: rows 1-9 settled as rows
25-34; #75 approved, #77 filed)_

## Inbox

Owner drop zone: bugs, findings, ideas, "next I want". One bullet each,
no format rules. Sessions triage at start: bugs to fixes, ideas to the
backlog, work to `active/` specs or Next up. Empty is the goal state.

## Awaiting owner

Open calls live in [decisions.md](decisions.md). Nothing is blocking:
rows 1-9 were all settled in the 2026-08-30/31 interview (rows 25-34).
Headlines: claim-at-start approved (#75; watcher threads un-gate when
tap-in names an in-flight thread and its ticket), TITAN and the
frontier map replace "craft" and the star map
([design-ui-target.md](active/design-ui-target.md)), queue aging plus
mobile dismiss filed as #77, the P2 console stays as-is until the
framework build, the tape stays killed, mobile Talk absorbs live mode.

## Next up (likely order)

1. RIG prototype follow-ups per
   [spec-rig-prototype.md](active/spec-rig-prototype.md) (owner is
   fatigued on the prototype; framework build first).
2. Framework build: work backwards from the prototype to the daemon and
   wire changes, then the shared mobile/desktop component refactor
   ([design-ui-target.md](active/design-ui-target.md)).
3. Rules hierarchy pass: run Lauren Tan's hierarchy over the AGENTS.md
   rules that are still prose only (playback-state mutation during
   tests, deploy step per layer).
4. Spine follow-ups: parse Next up into the tap-in digest, the
   one-`state/*`-per-issue lint, a `recommend`/`prioritize` routing-table
   entry with cost logging.
5. Conversational layer Stage 2
   ([design-conversational-layer.md](active/design-conversational-layer.md));
   Stage 1 shipped in code per
   [spec-interpreter-stage1.md](active/spec-interpreter-stage1.md),
   tracker roll-ups un-gated (decisions row 20).
6. Phone image attachments in live mode (#76 image-artifact bubbles).
7. Cross-persona spawn race and subagent-finish announce filtering
   (small daemon fixes, see the backlog).
8. Arcade buttons Phase 3b, Pico-driven LED feedback
   ([design-arcade-button-controller.md](active/design-arcade-button-controller.md));
   3a HID input shipped (#16).

## Someday

See [reference/ideas-backlog.md](reference/ideas-backlog.md): cmux
evaluation, Android wrapper app, mobile lip-sync, multi-agent conference
mode, Donnie avatar art cleanup, theme switching, the 10,000-ft view,
and more.

## Recently shipped

| When       | What                                                                  |
| ---------- | --------------------------------------------------------------------- |
| 2026-08-29 | **STATUS reworked to a thin index** (fleet `html-status` skill): the 561-line "Now" history deleted (git has it), rules it still carried folded into AGENTS.md, its decisions recovered as decisions.md rows 17-24; `docs/active/` pruned (Round C/D boards, briefs and kickoff prompts to `archive/`, the UI architecture docs to `shipped/`) |
| 2026-08-29 | **Rulebook split**: AGENTS.md (every agent, fleet convention) and CLAUDE.md (main-session model and delegate guidance) |
| 2026-08-27 | **Memory rework**: Claude Code auto-memory off for this repo (27 of 32 memory files never read in the surviving month), memory dir archived and deleted, CLAUDE.md rewritten in directional shape with dated history stripped, `docs/decisions.md` kept as a parking lot (rows 11-15) |
| 2026-08-29 | **Voice lab moved into its own private repo** (`github.com/dougiefresh49/voice-lab`): the pipeline scripts and `/reels` labeler that sat untracked in the public jellyfin-streamline repo, the two voice-lab docs, the casting handoffs (copies; comic-reader keeps its own), and the workspace metadata (verdicts, assignments, prototypes, `index.csv`, 356 WhisperX transcripts) synced one way by `scripts/sync-from-workspace.sh`. Audio (45 GB) stays in `~/Movies/library/voice-lab`. README carries a generated on-deck table (`node scripts/on-deck.mjs --write`): awaiting verdicts are He-Man 0/9, TMNT-2012 0/20, DC Crisis 0/12, Doom 7/14, War 2/8, Apocalypse 4/7, Amy (Boom), Bulk. Ownership: voice-lab owns extraction + casting, comic-reader keeps the EL slot manager for now (voice-lab decision 3 open). |
| 2026-08-21 | **Casting reels for EVERY universe built (overnight)** — TMNT-2012 (20 incl. villain supplement, Dogpound-Rahzar merged, Casey candidates), HeMan-1983 (9), Sonic (Chaotix 8 / Forces 8 / Prime Shadow+Sonic from owner-picked YouTube / Big / Blaze / Storm), and **DC ingested from the Seagate rips for the first time** (Crisis on Two Earths, Doom, War, Superman/Batman Apocalypse → 12/13/11/7 reels; per-film casts differ — Conroy/Daly in Doom+Apocalypse). Labeler `/reels` is now a universe index → collapsible per-universe pages, 2-col on desktop. Pipeline changes (uncommitted in jellyfin-streamline): per-show prototype files in voicelab-verify.py, DC mappings in voicelab-label.py, reusable `queue/embed-show.sh`, `ingest-dc.sh`, `ingest-sonic-prime.sh`. AWAITING OWNER: verdicts on ~10 reel sets, then EL re-clones. |
| 2026-08-21 | **DC reels rebuilt with the embedding-verified method** — owner: "tired of hand labelling mislabeled voices"; the four DC films had been built label-trusted (no voice check), which is why Doom Alfred/Cyborg leaked Batman and Crisis Batman mixed Owlman. New `jellyfin-streamline/scripts/ripper/voicelab-film-reels.py`: vision labels only seed self-trimmed centroids, then every clip is assigned by voice (strict + cluster-aware pass, mismatches evicted). Verification rates per label were 30-90% — e.g. War "Flash" 5/93, Doom "Cyborg" 27/75. Rebuilt 14/13/10/7 reels (Bane new; Shazam/Billy/Harbinger/GL-Crisis under floor); v1 reels + verdicts archived as `-v1 (label-trusted)`. Label-trusted reels are retired. AWAITING OWNER: re-judge DC on `/reels/dc`. |
| 2026-08-21 | **Bane (Dark Knight Rises) YouTube pull** — owner prefers Tom Hardy's Bane over the animated one; `queue/ingest-bane.sh` (yt-dlp 360p/96k SABR fallback again, 379s) → Demucs → WhisperX split Bane into two mask-filter clusters (02+03 → `speakers.json`, text-confirmed). The embedding pass evicted every long monologue for purity >0.6 (masked voice over Zimmer score — not a wrong speaker), so this single-voice item is ranked, not evicted: `Casting-Bane-TDKR/Bane.mp3` (31 clips/86s, cleanest first) + `Bane (long lines, score under).mp3` (10/56s) for the owner to judge whether the score residual is clone-safe. Shown on `/reels/dc` as "Bane (Dark Knight Rises)". |
| 2026-08-21 | **DC × Sonic crossover voice cast assembled** — owner judged the rebuilt DC reels (Doom 7 good, Apocalypse 4 good, War Cyborg; "use SB Apocalypse Batman") + Sonic sets. Cast sheet: `comic-reader/data/casting/dc-x-sonic-cast.{json,md}` (10 DC + 16 Sonic blessed, primary + alternates); clone-ready samples with the owner's trim notes applied (Forces Tails, Chaotix Charmy) at `voice-lab/clone-sources/dc-x-sonic/`. Gaps: Amy Rose + Metal Sonic have no source; Knuckles 35s / Silver 15s thin; Crisis + half of Doom/War/Apocalypse unjudged. Bane (TDKR) both reels APPROVED → combined 141s clone file is the primary Bane (Doom animated = alternate). NEXT (owner-gated): EL instant clones, registry entries. |
| 2026-08-21 | **Amy Rose (Sonic Boom) YouTube pull** — owner reasoning: Prime's actress voices every Shatterverse Amy variant (clone would average them) and the game register is shriller; Boom is Cindy Robinson's normal Amy. `queue/ingest-amy.sh` (491s source, single-speaker cluster → `Amy Rose`). Audition reel `Casting-Amy-SonicBoom/Amy Rose.mp3` (35 clips/88s) on `/reels/sonic` as "Amy (Boom)"; 170s clone file already in `clone-sources/dc-x-sonic/Sonic/`. AWAITING OWNER: verdict. Remaining cast gap: Metal Sonic only. |
| 2026-08-21 | **First voice-lab → ElevenLabs clone: Raphael 1990 v3** (`66KaORPuFP0qLgNZk7im`, Creator slots now 30/30). Owner A/B'd EL Audio Isolation on the 38s reel ("good mostly, not great in parts") → cloned from the RAW v3 reel, no noise removal; isolation didn't meter against the char quota. A/B file (same line, Raph v2 then the new clone) on `/reels/tmnt` under TMNT 1990 v3. EL inventory audit: 8 IVCs (2012 Donnie/Mikey/Splinter/Karai/Shredder, Raph v2, Narrator, Soldier) + 21 voice-designs (Leo, Bebop, Rocksteady, all Rangers, Zordon/Alpha/Rita/Zedd) — no April. TMNT wishlist gaps: Leo 1990 thin (32s; Ooze top-up possible — same Leo/Mikey actors, Raph/Donnie/April recast), Bay Bebop/Rocksteady need a source (Out of the Shadows not on the Seagate), April era TBD (1990 + 2012 both blessed). Slots are the constraint → delete/recreate voice-designs per archive-and-restore. |
| 2026-08-21 | **TMNT × MMPR casting handoff package** for the comic-reader agent — `comic-reader/data/casting/tmnt-x-mmpr/README.md` (cast table: clone file → existing EL voice → description/labels to carry → status casting/approved/ready-to-clone/cloned) + `cast.json` + `elevenlabs-voices-snapshot.json` (all 30 own voices with description/labels/settings, fetched read-only). 22 clone-ready files in `voice-lab/clone-sources/tmnt-x-mmpr/{TMNT,MMPR}/` with owner trims applied (2012 Rocksteady sfx, Bebop last clip). New Bulk reel built (unjudged). Owner rule: replacements must carry the old voice's description + labels (direct swap); DB/registry ID nuances belong to the comic-reader agent. Ready-to-clone: Leo 1990, 6 Rangers, Zordon, Alpha, Rita, Zedd, Goldar. Casting: April era, Bay Bebop/Rocksteady source, Baxter/Bulk verdicts, Skull/Squatt/Krang source. |
| 2026-08-21 | **Raph v3 live in the room for testing** — `66KaORPuFP0qLgNZk7im` added to `~/.cursor/tts/characters.json` (and the gitignored repo seed) as a second "Raphael" entry placed FIRST, so `team.sh Raphael` / the panel picker resolve to the 1990 clone while Raph v2 stays resolvable for existing sessions; daemon restarted (character registry is cached at load). To revert: delete the `66Ka…` entry. Handoff doc published to Postplan under `dougiefresh49/comic-reader` (draft q4xml21zq2sv). |
| 2026-08-21 | **Raph v3 APPROVED + tested in the room** (owner: "really good and clean"; the fast pacing was `default_speed: 1.5` = EL 1.2 + ffplay atempo 1.25, not the voice — pauses optimization deferred). Handoff docs + Postplan (q4xml21zq2sv v2) updated. Side experiment (owner theory): feed ElevenLabs-generated Raph v3 replays (128s, slowed to 1.0×, word-aligned transcripts) to Voicebox Qwen3-TTS as a local clone — result under TMNT 1990 v3 on `/reels/tmnt` for a verdict; if it holds up, the EL cast could be archived locally instead of occupying slots. |
| 2026-08-21 | **Voicebox-from-EL-replays theory CLOSED** — Raph v3 local clone from pristine EL output A/B'd against EL: owner "still junk". Reference was continuous clean dialogue, so the local model is the ceiling (postmortem addendum). Qwen model unloaded; profile left in Voicebox, harmless. |
| 2026-08-20 | **Casting reels BLESSED** — MMPR-1993 11/11 good after a verification pass (embeddings → prototypes → reassign; Jason owner-seeded) and TMNT-1990 re-cut from its transcript + assigned by voice (owner: "MUCH better… all just the single characters"). Next, owner-gated: ElevenLabs instant re-clones from the reels (`/reels` on the labeler). Details in the postmortem doc. |
| 2026-08-20 | **Voice-lab local-TTS lane CLOSED (pursued to the end, not viable)** — Voicebox (Qwen/Chatterbox/TADA) + VibeVoice 1.5B all owner-eared vs an ElevenLabs original; same-source control proved the corpus was never the bottleneck. EL stays the speaker, Gemini stays the writer. Salvage: casting reels for the text-designed EL voices (Casting-MMPR-1993 ×11 + Casting-TMNT-1990, on `/montages` for owner review → EL instant re-clone) and the voice-bible-from-corpus prompt idea. Non-Qwen Voicebox models deleted (+10GB tada-codec cache). Full findings: [reference/voice-lab-local-tts-postmortem.md](reference/voice-lab-local-tts-postmortem.md) |
| 2026-08-15 | **T3 replies Phase B — reply into T3 threads from mobile** (spec-t3-live-phase-b.md rev 2, built; ONE owner-gated step remains) — mobile composer now enabled for provisioned SDK (T3) sessions; replies POST `thread.turn.start` to T3's local server (`t3-reply.ts`: loopback-IP-literal origin validation, `redirect:error`, no-bearer liveness probe, 0600 bearer w/ owner+regular-file checks, fixed error codes, 6.8s timeout). Auth via `scripts/t3-provision-bearer.sh` (short-TTL admin mint → least-privilege pairing delegate → oauth exchange → 0600 bearer → REVOKE admin as commit gate). Credit guard: sdk branch stamps the phone-ack marker before dispatch (kept on ambiguous post-POST failures) so the turn's UserPromptSubmit never bills a dynamic voice response. Plan reviewed by Sol+grok; grok security-reviewed the diff (found 3 credit-guard issues, all fixed). **DONE overnight:** bearer provisioned + validated (exactly 2 scopes, admin revoked, orphan admin from a first failed run also revoked), `replyable` flips true, full path dry-run-verified through `GET threads/:id`. **OWNER-GATED:** the single real reply POST (wakes an agent + spends Anthropic quota) was intentionally NOT auto-run — see the morning note. Attachments = deferred stretch (composer is text-only). |
| 2026-08-15 | **T3 live mode Phase A — watch/listen + live-mute** (spec-t3-live-phase-a.md, shipped) — SDK-harness (T3 Code) sessions now get chat + go-live on the mobile room page (gate relaxed from team-only to `isTeamSession \|\| isSdkCard`). New live-mute toggle (ChatView header + CallView dock) watches the response as TEXT with ZERO auto Gemini/ElevenLabs spend: intermediates skipped, turn-finals fall back to announce/tap-to-play, plus a queued-before-mute race gate and a retire-before-mode-gate fix. Live text auto-refreshes in chat via a `lastEmitAt` heartbeat stamped at text-observation (covers held finals) → convo-state trailing-throttled `threadRev` bump. New `sdk`/`live.muted`/`lastEmitAt` protocol fields (all optional, deploy-skew safe) + `set_live_mute` command. Sol+grok reviewed the plan; built in 2 delegated worktrees (grok daemon, composer mobile). Verified: full free lane + in-browser (chrome-devtools: chat/go-live/mute-toggle/watching-treatment/auto-refresh) + 1 paid clip (unmuted sdk synth fires; mute mid-call stops the next). Phase B (replies into T3 threads) spec queued. |
| 2026-08-15 | **Chat round 2** — send from chat auto-starts live-muted (free text streaming of the response, T3-style; ends on sheet close unless user takes over), agent-state transitions refetch the thread (finals land in announce mode; 'sending…' resolves), play chips on every agent final (grant queued update / free replay / new billable `speak_text` on-demand synthesis, sparkle=generated vs play=replayed). Image-artifact bubbles → #76. Browser-verified + one bounded synthesis. |
| 2026-08-15 | **Chat reply-echo fix** — sent messages no longer vanish from the mobile chat between send and transcript landing (optimistic pending bubble with 'sending…', reconciled against /thread on match or 3-min TTL; affected tmux AND T3 replies). Browser-verified E2E vs a mock team session. |
| 2026-08-15 | **Phase B VALIDATED by owner** — first real phone→T3 reply landed (16:33 dispatch → phone ack, zero dynamic-response spend; credit guard held) and live mode enabled from the phone on the same session minutes later. spec-t3-live-phase-b moved to shipped/. |
| 2026-08-14 | **T3 Code sessions stay in the room + Settle-aware eviction** — SDK-harness sessions (T3 desktop, `entrypoint: sdk-*`) get an `sdk: true` marker in their state card; SessionEnd `reason=other` (T3's 30-min idle reaper, confirmed in pingdotgg/t3code `ProviderSessionReaper.ts`) and the daemon pid-reaper now KEEP those cards until 12h of inactivity (`SDK_CARD_TTL_MS`). T3's Settle button emits no harness-visible signal (settle = `settledOverride` reclassify + `thread.session.stop`, same `reason=other`), so the room reads T3's local store directly (`~/.t3/userdata/state.sqlite`, read-only: `resume_cursor_json.$.resume` → thread settled/archived/deleted): settled threads evict immediately via both the SessionEnd hook and each reaper sweep (`t3-thread-state.ts`). Terminal-session lifecycle unchanged. Verified free: hook sims (settled cleaned / unsettled kept / non-sdk cleaned) + reaper fakes (settled reaped, fresh sdk kept, 13h-stale sdk reaped) |
| 2026-08-02 | **Round D built into the prototype** — THE HANGAR (fleet zoom rung, floor bus, traffic strip, berth tabs, ⌘K switcher), commissioning bay with live-bound manifest + cost-honest dials, THE TOOL CRIB at /crib (generated 35-instrument manifest, provenance stamps, stock lamps, work orders), SERVICE SCHEMATIC at /map (harness lighting, wire terminals, gap rivets, shift-click part-number deep links); cmdk/dialog/tooltip/sonner/tabs/collapsible adopted — all three dead shadcn primitives now have consumers |
| 2026-07-30 | **RIG P2 "the console" shipped** — panel main window is the RIG console (token flip desktop-only, faceplate + Donnie bay, thread nodes + open-node history, crew manifest, watch chips); input parity landed: desktop typed chat (incl. the panel-ws inject fix) + attachments; ~520 lines of legacy style.css deleted |
| 2026-07-30 | **THE RIG: design target locked + P1 "metal shop" shipped** — design-ui-target.md signed off; --rig-* tokens, 12 RIG primitives + gallery in @room/ui, dock strip reskinned (salience LED bar, ticker, lamps); verified via gallery + codex dock screenshots |
| 2026-07-28 | **Spine validated + Round C concept round** — `state/*`+`gear/*` labels live on the tracker, free `tap-in.ts` harness, 11 questions graded twice (0 wrong answers after deterministic roll-up, <4¢ total); design brief locked and 4 blind concept boards + pick matrix published |
| 2026-07-28 | **#73 architecture concepts** — target layering picked (docs 00–07 in active/architecture-concepts/); docs-publish renders Mermaid → SVG; dock-runaway regression filed as #74 |
| 2026-07-27 | **Round A — audit criticals & quick wins** (#58–#61): perimeter closed (loopback+Tailscale bind, token rotation/no-log, skip-permissions opt-in, spawn-dir allowlist, minimal spawn env, tmux exact-match + pane guard), deploy safety (characters.json survives deploys, recorded repo-root, frozen-lockfile installs, scripts dir-sync), reliability (API timeouts+retry, failedCount badge, 64KB picker read → 0.98s→0.024s, atomic queue writes, periodic retention), hook-layer security (ingest session-id validation, .env allowlist, secrets/text out of argv, log hygiene, retention guard) |
| 2026-07-23 | Refactor Phases 6-7: legacy deletion (mobile.html, SwiftBar), audio/hid splits |
| 2026-07-22 | Mock live harness (free live-mode regression testing)                 |
| 2026-07-21 | Live mode v2 — call + chat views, /thread history, activity feed      |
| 2026-07-19 | Mobile v2.3 — live streaming to phone, speed button, launch flags     |
| 2026-07-08 | Room of Devs Phases 1-4 — panel, personas, replies, team sessions     |
